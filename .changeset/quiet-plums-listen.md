---
"json-schema-studio": patch
---

Unify the Monaco editor's font with the app's Inter sans-serif (instead of the default monospace) and fix the "Studio / JSON Schema" logo text rendering in monospace instead of the app's sans-serif
